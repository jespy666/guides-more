### Установка русской локали на VPS
1. Установить русскую локаль:  
`sudo locale-gen ru_RU.UTF-8` 
2. Сконфигурировать локали:  
`sudo dpkg-reconfigure locales` -> Выбрать по умолчанию русскую локаль (если требуется)